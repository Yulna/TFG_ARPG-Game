using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

//[CustomPropertyDrawer(typeof(StatVariable))]
//public class StatVariableDrawing : PropertyDrawer
//{


//    public override void OnGUI(Rect position, SerializedProperty property, GUIContent label)
//    {
//        base.OnGUI(position, property, label);
//       // EditorGUI.BeginProperty(position, label, property);

//        //position = EditorGUI.PrefixLabel(position, GUIUtility.GetControlID(FocusType.Passive), label);

//       // Rect base_rect = new Rect(position.x, position.y, 30, position.height);

//       // //What if veriable is not set (none in the inspector)
//       /// EditorGUI.PropertyField(base_rect, property., GUIContent.none);

//       // EditorGUI.EndProperty();
//    }
//}
